@model IEnumerable<MiCodeFirst_DataAnnotations.Models.Matricula>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Alumno.Apellidos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CursoID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Precio)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Alumno.Apellidos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CursoID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Precio)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.MatriculaID }) |
                @Html.ActionLink("Details", "Details", new { id=item.MatriculaID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.MatriculaID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
